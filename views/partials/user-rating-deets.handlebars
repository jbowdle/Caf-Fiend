<section class="card user-rating-deets">
    <h3>{{drink.beverage}}</h3>
    <p>Type: {{drink.bev_type}}</p>
    <p>Current Rating: {{drink.current_rating}}</p>
    <p>Your rating: {{rating}}</p>
    {{#if review}}
    <p class="review">{{review}}</p>
    <button data-drinkID="{{id}}" class="reviewDrinkBtn">Edit Review</button>
    {{else}}
    <button data-drinkID="{{id}}" class="reviewDrinkBtn">Add Review</button>
    {{/if}}
</section>